<Window x:Class="TravelAgency.View.OwnerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TravelAgency.View"
        mc:Ignorable="d"
        Title="OwnerView" Height="520" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="28*"/>
            <ColumnDefinition Width="161*"/>
            <ColumnDefinition Width="611*"/>
        </Grid.ColumnDefinitions>
        <TextBlock
            FontSize="18"
            FontWeight="Medium"
            Foreground="#68cbf8"
            TextWrapping="Wrap" Margin="2,32,535,446" Text="Owner" Grid.ColumnSpan="2" Grid.Column="1" />
        <Button x:Name="btnCreateAccommodation"
            Background="#68cbf8"
            Content="Create Accommodation"
            FontSize="10"
            Foreground="#ffffff" Margin="2,72,0,400" Click="btnCreateAccommodation_Click" Grid.Column="1" />
        <Button x:Name="btnLogout"
            Background="#68cbf8"
            Content="Logout"
            FontSize="10"
            Foreground="#ffffff" Margin="2,122,0,350" Click="btnLogout_Click" Grid.Column="1" />

    </Grid>
</Window>
